public class C extends B {
    public C(int i, int j) {
        // super()
        // If we don't type anything, it is as though we typed super().

        System.out.println("C's (int, int) constructor called");
    }
}